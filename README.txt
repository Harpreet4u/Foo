Foo README

Add redirect uri as http://localhost:6543 avoid adding / at the end

edit your clientID and clientSecret to "client_secrets.json" file


exports every file(publicy shared) in text format to index.pt...




